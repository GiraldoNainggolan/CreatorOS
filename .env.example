# TCOS / CreatorOS Monorepo Environment Template
# Note: Never commit actual secret keys or credentials to version control.

# Frontend
VITE_API_BASE_URL=/api

# Backend (Laravel)
APP_NAME=TCOS
APP_ENV=production
APP_DEBUG=false
APP_URL=https://your-domain.com
